# RL 基线实验记录

本文档只记录 Panda 动态线缆抓取的 PPO 强化学习实验，包括算法与接口演进、课程学习、训练评估、失败模式和复现实验信息。脚本策略、特权规则策略、DynamicVLA 等非 RL 实验不在本文记录。

> 更新日期：2026-08-28
> 当前主实验：`ppo_dlo_baseline_v6`
> 当前完整归档 checkpoint：3,801,216 steps
> 归档模型 SHA256：`77204581d77222ec472e8699a745de16ed79875315f4b68b7707dd51308e528e`
> 更新说明：本机另有 8.8M 固定四场景评估和 19.87M 训练曲线，因缺少完整归档证据，
> 分别标记为 `PROVISIONAL` 和训练进展。

## 1. 任务、接口与成功判定

策略从完整线缆状态中选择抓取动作，使 Panda 夹爪接近线缆中段、与局部线缆切向正确对齐，在充分插入后闭合，并抬升、稳定保持线缆。

| 项目 | 设置 |
|---|---|
| 观测 | 99 维：14 个弧长采样点的 TCP 相对位置与速度（84）+ 7 维关节位置 + 7 维关节速度 + 夹爪开度 |
| 动作 | 5 维：base-frame `xyz`、world-z `yaw`、夹爪控制量 |
| 算法 | PPO，MLP `[256, 256, 128]`，ReLU |
| rollout | 每个训练环境 `n_steps=1024` |
| batch / epoch | 512 / 5 |
| 折扣 | `gamma=0.995`，`GAE lambda=0.95` |
| 初始学习率 | `3e-4 → 3e-5` 线性退火 |
| 初始熵系数 | `0.01 → 0.001` 线性退火 |

### 1.1 成功判定

公共任务成功是跨方法比较和最终汇报采用的主指标。判定要求确认物理抓取、夹爪处于闭合状态，并连续保持 0.8 s，同时满足：

- 被抓线缆 body 高度大于 0.14 m；
- 至少 18% 的线缆节点高度大于 0.055 m；
- TCP 到目标的距离不超过 0.0825 m。

PPO 内部成功是训练诊断指标，除公共任务当前成功外，还要求原始双侧接触、pinch、至少 0.1 s 的 secured grasp、TCP 距离不超过 0.055 m，以及严格几何条件连续保持 0.8 s。

因此 PPO 内部成功比公共任务成功更严格，不应用来覆盖公共任务结论。视频和公共判定已确认：刚体场景 episode 12、形变场景 episode 13 均完成抓取且没有滑落，但内部成功为假；对应内部 hold 峰值仅为 0.54 s 和 0.40 s，属于诊断假阴性。

### 1.2 奖励设计

#### 距离和姿态对齐奖励

距离使用夹爪抓取中心到线缆“可抓取中段”最近点的距离；姿态对齐使用该最近点处的线缆局部切向
与夹爪闭合方向之间的角度。

- 接近进度：`6.0 ×（上一步距离 - 当前距离）`；
- 对齐进度：`2.0 × 对齐势能增量`。

#### 接触奖励

- 夹爪接触到线缆：`+0.15`；
- 当线缆距离足够近、姿态对齐且充分进入两指之间时闭合：`+0.75`，每回合只发放一次；
- 已满足闭合条件却继续张开：每步 `-0.001`；
- 线缆尚未充分进入夹爪就闭合：首次 `-0.04`，持续错误闭合时每步再扣 `-0.001`；
- 每次切换夹爪开闭状态：`-0.02`，用于抑制频繁开合抖动。

#### 夹持奖励

- **Pinch（双指夹持）**：左右两个夹指都与线缆发生有效接触，首次达到时奖励 `+0.25`；
- **Aligned pinch（对齐夹持）**：形成 pinch 时，夹爪闭合方向与线缆局部切向接近垂直，
  对齐分数至少为 `0.90`，额外奖励 `+1.0`；
- **Secured grasp（稳定夹持）**：在 pinch 基础上，夹爪开度和线缆位置合格，线缆被抬升至少
  `3 cm`，且双指接触连续保持 `0.1 s`，奖励 `+4.0`。

#### 最终成功奖励

达到 PPO 最终成功条件时奖励 `+25.0`，并结束当前回合。

## 2. 课程学习

| 阶段 | 场景 | 难度 |
|---|---|---|
| 0 | `id_static` | 静态 |
| 1 | `id_shape_nominal_current` 与 `id_rigid_l1_nominal` 分别采样 | `1/3 → 2/3 → 1` |
| 2 | `id_combined_l1_nominal` | `1/3 → 2/3 → 1` |

v3 以后使用严格评估门控。连续两次评估同时满足以下条件才升级：

- aligned pinch ≥ 50%；
- secured/stable grasp ≥ 20%；
- strict success ≥ 10%。

v6 在约 1.4M steps 进入阶段 1、难度 1/3。3.801M 归档训练结束时仍为阶段 1、难度 1/3，
升级确认计数为 1/2，未进入难度 2/3，也未进入组合动态阶段。

## 3. PPO 版本演进

主实验链为：

```text
ppo_dlo_baseline_v2 → v3 → v4 → v5 → v6
```

| 版本 | 关键改动 | 主要结果或问题 |
|---|---|---|
| v2 | 完整 DLO 状态、5 维任务空间动作、L1 课程 | 姿态轴语义错误，夹爪倾向沿线缆抓取 |
| v3 | 修正闭合方向；平移改为 base frame，偏航改为 world-z；引入严格课程门控 | 姿态正确，但连续夹爪控制没有学会可靠闭合 |
| v4 | 捕获感知的迟滞开闭：`<-0.35` 闭合，`>+0.35` 张开 | 解决不闭合，但经常过早闭合、浅夹持后滑落 |
| v5 | 增加指间捕获走廊和插入深度闭合门控 | 过早闭合惩罚过强，策略塌缩为几乎始终张开 |
| v6 | 保留几何门控，降低并限制过早闭合惩罚，增加等待闭合惩罚 | 恢复闭合和抓取；静态显著改善，动态泛化仍弱 |

v6 相对 v5 的核心奖励修改：

- capture-ready 时保持张开的惩罚为 `-0.001/step`；
- 过早闭合事件惩罚由 `-0.25` 降为 `-0.04`；
- 持续过早闭合惩罚由 `-0.003/step` 降为 `-0.001/step`；
- 每回合过早闭合事件惩罚总额上限为 0.50。

## 4. v2–v5 历史结果

下表只保留解释版本演进所需的 PPO 主线结果。训练严格评估使用当时课程分布，不等同于固定四场景测试。

| 版本 / checkpoint | 当前课程 | strict success | pinch | aligned pinch | loaded lift | stable grasp | 备注 |
|---|---:|---:|---:|---:|---:|---:|---|
| v2 best，1.2M | 静态 | 8.3%（2/24） | 50% | — | — | 8.3% | 姿态目标仍有错误 |
| v2 final，2M | 静态 | 0% | 50% | — | — | 0% | 后期退化 |
| v3 final，2M | 静态 | 0% | 0% | 0% | 0% | 0% | 姿态正确但不闭合 |
| v4，1.5M | 静态 | 50% | 90% | 70% | 65% | 65% | 随后升级至阶段 1 |
| v4 final confirmation | 阶段 1，难度 1/3 | 20% | 30% | 25% | 25% | 25% | `N=20` |
| v5 final，2M | 静态 | 0% | 5% | 5% | 0% | 0% | 始终张开塌缩 |

v4 best 的固定四场景测试为 4/40（10%）：static 1/10、rigid 3/10、shape 0/10、combined 0/10。v5 final 为 0/40，其中 35 回合 never pinch。两组结果共同支持“v4 闭合过早、v5 惩罚过强”的版本诊断。

## 5. v6 初始训练：0–2M steps

v6 从头训练 2M steps，在 1.4M 左右首次满足静态阶段升级条件，之后进入阶段 1、难度 1/3。

| checkpoint | 评估分布 | success | pinch | aligned | lift attempt | loaded | secured | mean return |
|---:|---|---:|---:|---:|---:|---:|---:|---:|
| 1,400,448 | 静态 | 40% | 95% | 65% | 70% | 45% | 40% | 16.904 |
| 2,000,640 | 阶段 1，难度 1/3 | 20% | 50% | 40% | 45% | 30% | 25% | 10.412 |
| 2,000,640 confirmation | 阶段 1，难度 1/3 | 20% | 45% | 30% | 40% | 30% | 25% | 9.250 |

该阶段 best checkpoint 的固定四场景测试每场景 10 回合：

| 场景 | 公共任务成功 |
|---|---:|
| static | 5/10 |
| rigid L1 | 0/10 |
| shape | 2/10 |
| combined L1 | 0/10 |
| **总计** | **7/40（17.5%）** |

结果表明 v6 已修复 v5 的始终张开塌缩，但 2M 时主要能力仍集中在静态场景。

## 6. v6 续训：2M–4M steps

续训从 2,000,640-step best checkpoint 开始，目标增加约 2M steps。由于并行 rollout 对齐，最终到约 4,015,872 steps。续训采用较保守的 schedule：学习率约 `3.46e-5 → 3e-5`，熵系数约 `0.00115 → 0.001`。

| checkpoint | success | pinch | aligned | lift attempt | loaded | secured | mean return | 说明 |
|---:|---:|---:|---:|---:|---:|---:|---:|---|
| 2,800,896 | 45% | 65% | 55% | 65% | 45% | 45% | 20.473 | 达到升级指标一次 |
| 3,000,960 | 40% | 55% | 30% | 50% | 45% | 45% | 18.507 | aligned 回落 |
| 3,701,184 | 50% | 60% | 40% | 60% | 50% | 50% | 22.331 | 动态能力恢复 |
| 3,801,216 | 65% | 70% | 50% | 70% | 65% | 65% | 28.330 | routine eval，续训最佳 |
| 3,801,216 confirmation | 45% | 60% | 50% | 60% | 55% | 55% | 21.364 | **当前 best_model** |
| 4,001,280 | 40% | 65% | 50% | 60% | 45% | 45% | 19.150 | 未超过 best |

最终课程仍停留在阶段 1、难度 1/3，合格评估计数 1/2。也就是说，策略尚未稳定到足以推进课程，不应描述为已学到组合动态阶段。

训练末段控制诊断：

| 指标 | 数值 |
|---|---:|
| gripper closed fraction | 64.9% |
| capture-ready fraction | 2.85% |
| capture-close event fraction | 0.0305% |
| premature-close event fraction | 2.94% |
| 动作维 0–4 饱和率 | 50.8%、32.6%、33.4%、34.6%、47.2% |

动作饱和率表示策略输出落在动作限幅区附近的比例。多个维度长期处于 30%–51%，说明策略频繁要求最大幅度运动或夹爪控制；这不等于直接失败，但提示控制较激进，并可能限制精细对准和插入。

## 7. 3.801M best 固定四场景评估

### 7.1 设置

- checkpoint：`outputs/rl/train/ppo_dlo_baseline_v6/evaluation/best/best_model.zip`
- checkpoint steps：3,801,216
- 每场景：20 个确定性回合
- seeds：`20270804–20270823`
- 输出：`outputs/rl/eval/v6_best_3p8m_20ep/`
- 评估 manifest Git：`6ecf169a1cec053879139c25a427b2f76c22750c`，工作树为 dirty

### 7.2 汇总

| 场景 | 公共成功 | PPO 内部成功 | pinch | aligned | secured | loaded | mean return |
|---|---:|---:|---:|---:|---:|---:|---:|
| `id_static` | 12/20（60%） | 12/20 | 17/20 | 15/20 | 15/20 | 17/20 | 28.329 |
| `id_rigid_l1_nominal` | 4/20（20%） | 3/20 | 6/20 | 5/20 | 4/20 | 5/20 | 11.465 |
| `id_shape_nominal_current` | 3/20（15%） | 2/20 | 9/20 | 4/20 | 4/20 | 5/20 | 6.658 |
| `id_combined_l1_nominal` | 0/20（0%） | 0/20 | 2/20 | 2/20 | 1/20 | 2/20 | 2.016 |
| **总计** | **19/80（23.75%）** | **17/80（21.25%）** | **34/80** | **26/80** | **24/80** | **29/80** | — |

三个动态场景合计公共成功 7/60（11.7%）。60 个动态回合中有 43 个 never pinch，说明当前首要瓶颈是动态条件下未能形成初始夹持，而不是夹住以后主动张开。全部 80 回合均未出现 `active_open_after_secured`。

### 7.3 失败类型

| 场景 | never pinch | pinch but not secured | secured but no strict/internal | physical slip |
|---|---:|---:|---:|---:|
| static | 3 | 2 | 0 | 3 |
| rigid L1 | 14 | 2 | 1 | 0 |
| shape | 11 | 5 | 2 | 0 |
| combined L1 | 18 | 1 | 0 | 1 |

其中“secured but no strict/internal”包含公共任务已成功、但 PPO 内部严格保持计时没有通过的诊断假阴性。公共任务成功仍是最终主指标。

### 7.4 与 2M checkpoint 的同 seed 对比

只比较两次评估共有的前 10 个 seeds：

| 场景 | 2M best | 3.801M best |
|---|---:|---:|
| static | 5/10 | 8/10 |
| rigid L1 | 0/10 | 1/10 |
| shape | 2/10 | 1/10 |
| combined L1 | 0/10 | 0/10 |
| **总计** | **7/40** | **10/40** |

续训的确定性同 seed 净改善主要来自静态场景。20 回合总体结果说明 rigid 和 shape 已出现少量成功，但 combined 仍未成功；当前证据不支持“动态课程已经学会”。

### 7.5 评估过程说明

最初两次并行评估在首个 episode 后退出，原因是 `evaluate.py` 的日志打印引用了未定义变量 `failure_type`。服务器仅将该打印变量修正为 `policy_failure_type`，未修改环境、动作、奖励或指标判定。上述汇总只使用修复后完整结束的 80 回合，失败的部分运行未纳入统计。

## 8. 8.8M 暂定评估与 19.87M 训练进展

### 8.1 8.8M best 固定四场景评估

本机服务器日志副本 `../linux_log/v6_resume_8p8m_best_20ep/` 包含四个场景各 20 个
确定性回合，seeds 与 3.801M 评估相同，均为 `20270804–20270823`。

| 场景 | 公共成功 | pinch | aligned | secured | loaded |
|---|---:|---:|---:|---:|---:|
| `id_static` | 15/20（75%） | 20/20 | 13/20 | 16/20 | 17/20 |
| `id_rigid_l1_nominal` | 19/20（95%） | 19/20 | 15/20 | 19/20 | 19/20 |
| `id_shape_nominal_current` | 7/20（35%） | 12/20 | 5/20 | 7/20 | 9/20 |
| `id_combined_l1_nominal` | 4/20（20%） | 6/20 | 4/20 | 5/20 | 6/20 |
| **总计** | **45/80（56.25%）** | **57/80** | **37/80** | **47/80** | **51/80** |

与 3.801M 做同 seed 配对，8.8M-only 成功与 3.801M-only 成功分别为：static `4/1`、
rigid `15/0`、shape `6/2`、combined `4/0`，合计为 `29/3`，另有 48 个 ties。
改进主要来自 rigid；combined 虽从 0/20 提升到 4/20，仍是最弱场景。

该日志副本没有随仓库保存 checkpoint SHA256、训练配置、完整 manifest 和 source hash，
因此本节只能作为 `PROVISIONAL` 阶段结论。补齐证据前，3.801M 仍是当前完整归档模型。

### 8.2 19.87M 训练曲线

`../linux_log/v6_full_19p87m_curves/training_metrics_complete.csv` 记录到约
19,873,392 steps。最后 5000 个训练 episode 的窗口统计约为：公共成功 59.1%、pinch
69.3%、aligned pinch 45.1%、loaded lift 63.5%、grasped 62.7%。

这些数值混合当前训练分布，不能与固定四场景结果直接比较。目前没有与该训练末端对应的
四场景严格评估、checkpoint 哈希和课程状态归档，因此不能称为新的 best。

### 8.3 阶段 2 续训的静态遗忘与课程回放修改

> 2026-08-26：修改已实现，待训练验证。

后续续训已推进到阶段 2、组合动态难度 1/3。原课程实现每次升级时都会使用新阶段场景完全替换
之前的训练场景。进入后期动态阶段后，固定场景评估中的静态能力出现回落：

| 指标 | 修改前 | 后期续训 | 变化 |
|---|---:|---:|---:|
| 公共成功 | 85% | 75% | -10 pp |
| aligned pinch | 15/20 | 13/20 | -2 |
| secured | 18/20 | 16/20 | -2 |
| pinch-but-not-secured | 1/20 | 4/20 | +3 |

这些结果说明后期只训练动态场景存在遗忘风险。本次仅修改课程采样，不改变观测、动作、奖励或
成功判定。修改后的训练分布为：

| 当前阶段 | 静态 | 形变 | 刚体 | 组合 |
|---|---:|---:|---:|---:|
| 阶段 0：静态 | 100% | 0% | 0% | 0% |
| 阶段 1：单项动态 | 20% | 40% | 40% | 0% |
| 阶段 2：组合动态 | 10% | 10% | 10% | 70% |

严格课程评估仍只覆盖当前阶段目标：阶段 1 评估形变和刚体，阶段 2 评估组合。静态和单项动态
回放仅用于抗遗忘，不会直接抬高晋级成功率。当前只有代码修改，尚无新训练结果；后续需要沿用
固定四场景、每场景 20 回合的协议验证课程回放是否有效。

## 9. 当前结论与下一步

1. v6 奖励重平衡有效修复了 v5 的始终张开塌缩，3.801M best 的静态公共成功率达到 60%。
2. 2M–3.801M 这一段续训把同 seed 总成功从 7/40 提高到 10/40，收益主要来自静态场景。
3. 3.801M 时课程仍为阶段 1、难度 1/3；后续训练曾推进到阶段 2，但出现静态遗忘迹象，
   因而加入了尚待独立验证的课程回放。
4. 动态场景的主失败模式是 never pinch。下一版应优先改善动态目标预测、接近与对齐，而不是继续加大夹持后的保持奖励。
5. 8.8M 暂定结果显示 rigid 已显著改善，但 combined 仍只有 4/20，shape 也只有 7/20；
   下一轮应继续固定四场景、固定 seeds 评估，并同时报告公共成功与 PPO 内部诊断。
6. PPO 内部严格计时会漏记部分视觉和公共判定已确认的成功。应修正其诊断语义或单独命名，但不能回溯篡改公共任务结果。
7. 19.87M 训练必须补做严格评估；只看训练窗口成功率不足以判断是否优于 8.8M。

### DLO 状态输入与真机迁移问题

现有 DLO 操作研究中的 RL 方法，大多将 DLO 状态作为策略输入，例如绳子上若干节点的位置。
本项目当前的 PPO 也采用这种设置：99 维观测中的主要部分来自 14 个弧长采样点的位置和速度，
仿真时可以直接从环境获得这些状态。

真机无法直接获得完整 DLO 状态，通常需要使用视觉方法估计线缆节点的位置和运动。但与静态线缆
相比，实时识别和跟踪持续运动、形变中的 DLO 状态更加困难。后续需要研究适用于动态 DLO 的
实时感知方法，或者改为将图像特征直接输入 RL 策略。

直接使用图像特征又会引入更大的 sim-to-real 差异；同时，RL 需要大量交互，在真机上直接训练
成本高、效率低且难以保证安全。因此，如何从当前依赖仿真 DLO 状态的 PPO 迁移到真机，仍是尚未
解决的主要问题。

## 10. 比较与复现限制

- 训练严格评估使用“当前课程分布”，固定四场景评估使用单一指定场景，两者成功率不能直接等同；
- 不同版本的奖励、评估器和诊断字段有演进，缺失字段不能按 0 处理；
- 当前训练只有一个主 seed，20 回合单场景成功率的统计区间仍较宽；
- best checkpoint 由 PPO 内部评估选择，不保证恰好是公共四场景综合最优；
- 评估 manifest 标记为 dirty，因为服务器有日志打印 hotfix；复现时应保留模型 SHA256 和 manifest；
- 视频用于复核事件语义，最终结果以公共任务判定、逐回合日志和物理状态共同确认。

## 附录：关键 RL Git 提交

| 提交 | 内容 |
|---|---|
| `6a6502d` | 初始 RL 实现 |
| `5379b80` | 用摩擦接触替代弹性抓取代理 |
| `ee9c530` | 完整 DLO 状态、任务空间动作与 L1 课程基线 |
| `794d766` | 修正抓取姿态并加强抬升学习 |
| `c5bcd60` | 并行严格评估 |
| `6992d6a` | 捕获感知迟滞夹爪控制 |
| `756c591` | 基于线缆插入深度的闭合门控 |
| `3506f63` | 重平衡过早闭合奖励，防止始终张开塌缩 |

当前目标和问题见[项目状态](project_status.md)，统一实验目录与评估约定见
[实验协议](reference/experiment_protocol.md)，实验可信度和产物索引见[实验索引](experiments/README.md)。
